import { createContext } from "react";

// type LocationCoordinateContextProps = {
//   locationCoordinate: {};
//   setLocationCoordinate: (lng: number, lat: number) => void;
// };

export const LocationCoordinateContext = createContext<any>(null);
